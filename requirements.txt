# ==============================================================================
# You MUST use Python 3.12 (or older 64-bit builds) for this project!
# ==============================================================================

# Core data processing (optimized versions)
pandas>=2.0.0
numpy>=1.24.0

# Financial data
yfinance>=0.2.0
alpaca-py>=0.7.0

# News scraping
gnews>=0.3.0

# Hybrid sentiment analysis: VADER + DistilRoBERTa
--extra-index-url https://download.pytorch.org/whl/cu118

vaderSentiment>=3.3.2
transformers>=4.30.0

# Apple Silicon (macOS) - Native processing
torch>=2.4.0; sys_platform == 'darwin'
torchvision>=0.19.0; sys_platform == 'darwin'

# Linux / Windows - CUDA 11.8 processing
torch==2.4.1+cu118; sys_platform != 'darwin'
torchvision==0.19.1+cu118; sys_platform != 'darwin'

# Utilities
pytz>=2023.3
python-dotenv>=1.0.0
tqdm>=4.65.0

# Visualization
matplotlib>=3.7.0